# Dedicated identity for the runner pod. The runner executes untrusted workflow
# code and has no reason to reach the Kubernetes API, so it gets no RBAC and no
# mounted token. Grant scoped permissions here only if a workflow genuinely
# needs them.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: github-runner
  namespace: github-runners
automountServiceAccountToken: false
